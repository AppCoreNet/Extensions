<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <PackageTags>$(PackageTags);Microsoft.Extensions.Hosting</PackageTags>
  </PropertyGroup>
</Project>